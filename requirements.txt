# FlowCast v1.0 — pinned to the versions the reported results were produced on.

# Core
numpy>=2.0
pandas>=2.2
pyarrow>=15.0          # parquet round-trips; without it the pipeline falls back
                       # to gzipped CSV and loses dtypes on reload
PyYAML>=6.0
scipy>=1.11

# Classical ML
scikit-learn>=1.5
xgboost>=2.0
joblib>=1.3

# Deep learning (CPU build is sufficient; the LSTM trains in ~3 min on one core)
torch>=2.2

# EDA figures
matplotlib>=3.8
seaborn>=0.13

# Dashboard
streamlit>=1.40
plotly>=5.20

# Tests
pytest>=8.0
