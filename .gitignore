__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
.venv/
venv/

# Data and artefacts are regenerated by the pipeline, not versioned.
data/interim/
data/processed/
models/*.joblib
models/*.pt
logs/*.log
reports/figures/*.png

# Raw inputs are versioned deliberately — the results are only reproducible
# against these exact files.
!data/raw/*.csv
